"use strict";
function combine(a, b) {
    if (typeof a === "number" && typeof b === "number") {
        return (a + b);
    }
    else {
        return (a + b);
    }
}
let result = combine(10, "Hello");
